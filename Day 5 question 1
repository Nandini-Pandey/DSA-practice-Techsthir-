class Solution {
    public int lastIndex( String s) {
        char ch[]=s.toCharArray();
        int idx=-1;
        int n=ch.length;
        for(int i=0; i<n; i++){
            if(ch[i]=='1'){
                idx=i;
            }
        }
        return idx;
        
        
        
    }
}
